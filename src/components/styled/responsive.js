const size = {
    sm: '576px',
    md: '768px',
    lg: '992px',
    xl: '1200px',
}

export const device = {
    deviceS : `(min-width: ${size.sm})`,
    deviceM : `(min-width: ${size.md})`,
    deviceL : `(min-width: ${size.lg})`,
    deviceXl : `(min-width: ${size.xl})`,
}